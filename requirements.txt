# ---- ingestion (Stage 1) ----
pymupdf>=1.24            # .pdf
python-docx>=1.1         # .docx  -- 122 of the 220 files
lxml>=5.0                # .xml   (structured resumes)
ftfy>=6.2                # mojibake repair
python-dateutil>=2.9     # varied date formats
rapidfuzz>=3.9           # fuzzy skill + section-header matching

# ---- matching (Stages 4-6, 9) ----
rank-bm25>=0.2.2
numpy>=1.26
scipy>=1.13              # spearman / ndcg
scikit-learn>=1.5

# ---- models (Stages 5, 7) ----
torch>=2.6               # install the CPU wheel (see README) - CUDA buys nothing for 33M/22M-param models
sentence-transformers>=3.3
huggingface-hub>=0.26    # explicit, resumable model download

# ---- explanations + chat (Stage 8, bonus 2) ----
groq>=0.13
python-dotenv>=1.0

# ---- demo ----
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12 # required by the upload endpoint

# ---- terminal output ----
rich>=13.9

# ---- optional: scanned-PDF OCR fallback ----
# pytesseract>=0.3.13    # also needs: sudo dnf install tesseract
# pillow>=11.0
