# Copy to .env and fill in. Free key from https://console.groq.com
# Used ONLY to phrase explanations and chat answers from facts the engine computed.
# Without it everything still works via the deterministic template fallback.
GROQ_API_KEY=gsk_your_key_here
